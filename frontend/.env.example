# API Configuration
# These variables control API endpoint selection

# Use local backend (true) or AWS Lambda (false/unset)
# Default: false (uses AWS Lambda)
NEXT_PUBLIC_USE_LOCAL_BACKEND=false

# Force AWS Lambda even in development mode
# Default: false (allows local backend in development)
NEXT_PUBLIC_FORCE_AWS=false

# API URL override (optional)
# If not set, will use dynamic endpoints from dynamic-endpoints.ts
NEXT_PUBLIC_API_URL=

# WebSocket URL override (optional)
# If not set, will use dynamic endpoints from dynamic-endpoints.ts
NEXT_PUBLIC_WS_URL=