# 🌐 PUBLIC TESTS URL
# URL base para public-tests (desarrollo local vs producción)
NEXT_PUBLIC_PUBLIC_TESTS_URL=http://localhost:5173

# 🔌 API ENDPOINTS
# URLs de la API backend (desarrollo local vs AWS Lambda)
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_WS_URL=ws://localhost:3001

# 🎯 CONFIGURACIÓN DE ENTORNO
# Variables de control de entorno
NEXT_PUBLIC_USE_LOCAL_BACKEND=true
NEXT_PUBLIC_FORCE_AWS=false
NODE_ENV=development

# 📋 INSTRUCCIONES:
# 1. Copia este archivo a .env.local para desarrollo
# 2. Las variables se detectan automáticamente según el entorno
# 3. Frontend local (localhost:3000) → Public-tests local (localhost:5173)
# 4. Frontend desplegado → Public-tests desplegado (CloudFront)
