{
  "name": "emotiox-v2",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build:shared && npm run build --workspace=frontend",
    "start": "npm run start --workspace=frontend",
    "build:shared": "npm run build --workspace=shared",
    "backend:dev": "npm run start --workspace=backend",
    "backend:build": "npm run build:shared && npm run build --workspace=backend",
    "backend:deploy:dev": "npm run build:shared && npm run check-aws dev && npm run deploy:dev --workspace=backend",
    "backend:deploy:test": "npm run build:shared && npm run check-aws test && npm run deploy:test --workspace=backend",
    "backend:deploy:prod": "npm run build:shared && npm run check-aws prod && npm run deploy:prod --workspace=backend",
    "backend:logs:dev": "npm run logs:dev --workspace=backend",
    "backend:logs:test": "npm run logs:test --workspace=backend",
    "backend:logs:prod": "npm run logs:prod --workspace=backend",
    "frontend:dev": "npm run build:shared && npm run dev --workspace=frontend",
    "frontend:build": "npm run build:shared && npm run build --workspace=frontend",
    "frontend:start": "npm run start --workspace=frontend",
    "frontend:deploy:dev": "npm run build:shared && npm run build --workspace=frontend && echo 'Implementar despliegue a S3/CloudFront'",
    "check-aws": "node backend/scripts/check-aws-credentials.js",
    "setup:dev": "npm run setup:iam:dev && npm run setup:env:dev && npm install && npm run setup:infra:dev",
    "setup:test": "npm run setup:iam:test && npm run setup:env:test && npm install && npm run setup:infra:test",
    "setup:prod": "npm run setup:iam:prod && npm run setup:env:prod && npm install && npm run setup:infra:prod",
    "setup:iam:dev": "node scripts/setup-iam-user.js dev",
    "setup:iam:test": "node scripts/setup-iam-user.js test",
    "setup:iam:prod": "node scripts/setup-iam-user.js prod",
    "setup:env:dev": "node scripts/setup-env.js dev",
    "setup:env:test": "node scripts/setup-env.js test",
    "setup:env:prod": "node scripts/setup-env.js prod",
    "setup:infra:dev": "cd infrastructure && npm run deploy:dev",
    "setup:infra:test": "cd infrastructure && npm run deploy:test",
    "setup:infra:prod": "cd infrastructure && npm run deploy:prod",
    "deploy:dev": "npm run validate:dev && npm run backend:deploy:dev && npm run update-frontend:dev && npm run frontend:deploy:dev && npm run test:smoke:dev",
    "deploy:test": "npm run validate:test && npm run backend:deploy:test && npm run update-frontend:test && npm run frontend:deploy:test && npm run test:smoke:test",
    "deploy:prod": "npm run validate:prod && npm run backend:deploy:prod && npm run update-frontend:prod && npm run frontend:deploy:prod && npm run test:smoke:prod",
    "update-frontend:dev": "node scripts/update-frontend-config.js dev",
    "update-frontend:test": "node scripts/update-frontend-config.js test",
    "update-frontend:prod": "node scripts/update-frontend-config.js prod",
    "validate:dev": "node scripts/validate-env.js dev",
    "validate:test": "node scripts/validate-env.js test",
    "validate:prod": "node scripts/validate-env.js prod",
    "test:smoke:dev": "node scripts/smoke-test.js dev",
    "test:smoke:test": "node scripts/smoke-test.js test",
    "test:smoke:prod": "node scripts/smoke-test.js prod",
    "rollback:dev": "node scripts/rollback.js dev",
    "rollback:test": "node scripts/rollback.js test",
    "rollback:prod": "node scripts/rollback.js prod"
  },
  "devDependencies": {
    "aws-sdk": "^2.1692.0",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.3",
    "serverless": "^3.40.0",
    "serverless-dotenv-plugin": "^6.0.0",
    "serverless-export-outputs": "^1.0.2",
    "serverless-offline": "^13.3.0"
  },
  "dependencies": {
    "node-fetch": "^2.7.0",
    "uuid": "^11.1.0"
  }
}
